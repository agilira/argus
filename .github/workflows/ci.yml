name: CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    uses: agilira/workflow/.github/workflows/go-ci-full.yml@main
    with:
      go-version: 'stable'
      gosec-config: '.gosec.json'
